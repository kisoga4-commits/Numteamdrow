// Central configuration values shared across modules.
export const APP_CONFIG = {
  canvasWidth: 960,
  canvasHeight: 540,
  minBrushSize: 1,
  maxBrushSize: 60,
  autosaveDelayMs: 700,
  defaultFps: 8,
  maxFrames: 300
};
